server:
  port: 8083

spring:
  application:
    name: enrollment-service
  data:
    mongodb:
      uri: ${MONGO_URI}

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    in.cloudblitz.enrollment: DEBUG
